Artful Portfolio — Vite + Vue 3

Setup

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

Build

```bash
npm run build
npm run preview
```

This project uses `gsap` for animations and a simple canvas background.
